# 🔧 REST API Документация

## Базовый URL

## Аутентификация

### Получение токена
```http
POST /auth/login
Content-Type: application/json

{
  "username": "ваш_логин",
  "password": "ваш_пароль"
}
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "name": "Иван Иванов",
    "role": "admin"
  }
}
Основные endpoints
📊 Получение статистики
http
GET /dashboard/stats
Authorization: Bearer {token}
📅 Работа с сменами
http
GET /shifts
POST /shifts
GET /shifts/{id}
PUT /shifts/{id}
🏥 Медицинские данные
http
GET /employees/{id}/medical
POST /medical/exams
GET /medical/expiring
🎥 Видеонаблюдение
http
GET /cameras
GET /cameras/{id}/stream
POST /cameras/{id}/screenshot
Пример использования
javascript
// Получение списка камер
const response = await fetch('/api/v1/cameras', {
  headers: {
    'Authorization': 'Bearer ' + token,
    'Content-Type': 'application/json'
  }
});

const cameras = await response.json();
Коды ответов
200 - Успех

401 - Не авторизован

403 - Доступ запрещен

404 - Не найдено

500 - Ошибка сервера

text

## 📋 ЧТО ДАЛЬШЕ:

1. **Создать структуру папок** `/docs/`
2. **Наполнить основные разделы** руководства
3. **Добавить API документацию** для интеграций
4. **Создать FAQ** с частыми вопросами
5. **Добавить скриншоты** и примеры

**Начнем с создания структуры? Или есть конкретный раздел который нужен в первую очередь?** 📝

P.S. Документацию можно разместить по адресу: `http://spkauto.online/chop_system/docs/`
